name: test env variables
on: workflow_dispatch
env:
  NAME: "Karabo"

jobs:
  test-env-varible:
    env:
      NAME: "Charles"
    runs-on: Ubuntu-latest
    steps:
      - name: print NAME env variable
        env:
          NAME: "Matsepane"
        run: echo "My name is $NAME"