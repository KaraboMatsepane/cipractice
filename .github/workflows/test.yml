name: full pipeline test
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build step
        run: echo "hello ${{ vars.MY_NAME }} h"

