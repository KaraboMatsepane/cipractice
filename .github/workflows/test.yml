name: Test
on: push
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          python-version: "3.11"
      - run: python --version
      - run: echo "Hello from pipeline"
      - run: echo "another message"
      - run: cat note.txt